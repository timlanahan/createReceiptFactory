/**
 * Factory for receipts that accepts some initialItems
 * A receipt should AT LEAST be able to: 
 * 1. add new items
 * 2. remove items
 * 3. get items
 * 4. sort items
 * 5. calculate the total cost of all items
 * 
 * interface Item {
 *   name: string;
 *   price: number;
 * }
 * 
 * @param {*} initialItems 
 * @returns 
 */
export const createReceiptFactory = (initialItems) => {

};
